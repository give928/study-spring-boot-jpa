#spring:
#  datasource:
#    url: jdbc:h2:mem:/study-spring-boot-jpa
#    driver-class-name: org.h2.Driver
#  jpa:
#    hibernate:
#      ddl-auto: create-drop
#    properties:
#      hibernate:
##        show_sql: true # System.out 으로 로그 출력
#        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace
